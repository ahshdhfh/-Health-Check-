<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ooori.healthcheck">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".imminsuActivity" android:label="imminsu 액티비티" />
        <activity android:name=".stratchActivity" android:label="stratch 액티비티" />
        <activity android:name=".proteinActivity" android:label="protein 액티비티" />
        <activity android:name=".partActivity" android:label="part 액티비티" />
        <activity android:name=".foodActivity" android:label="food 액티비티" />
        <activity android:name=".standardActivity" android:label="standard 액티비티" />
        <activity android:name=".timerActivity" android:label="timer 액티비티" />
        <activity android:name=".watchActivity" android:label="watch 액티비티" />
        <activity android:name=".calcuActivity" android:label="calcu 액티비티" />
        <activity android:name=".calenActivity" android:label="calen 액티비티" />
    </application>

</manifest>